# TODO

 - Clicking on a Tag will show the Transactions with that Tag
     - Selecting a different Tag, shows those transactions
     - But what if I wanted to see all Amazon AND Video transactions 
        - IT would take the subset, instead of a new list. 
        - If you select the Tag on the Left list of Tags, you get all TX with that TAG
        - If you select the Tag under a transaction on the Right side, it does a subset of the TX list with that tag
        - OR CMD-Mouse on Tag provides a subset.

 - how do I add a cashflow properties, like end of cycle by date or transaction.
        - defaults to start on first of the month
        - Cashflow menu allows switch to different start date

 - create statement based personal budget for me
        - Create NEW db
        - Save db


## Web version

This allows the ad to be blocked in the browser.
So we would need to charge monthlyfor this.

 - implement firebase AI for web app (see firebase-AI.md)

## future

 - add Firebase ads (see firebase-ads.md)
 - add Firebase Analytics
 - add Firebase Crashlytics
